jest.resetModules()
jest.resetAllMocks()
jest.clearAllMocks()


const useDimensions = require('../useDimensions')

describe('useDimensions', () => {

  describe('useDimensions', () => {
    it('', () => {
      
    })
  })
  
})
